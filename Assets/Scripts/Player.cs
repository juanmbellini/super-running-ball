using UnityEngine;

/// <summary>
/// Class to be used to mark that a game object is a player.
/// </summary>
public class Player : MonoBehaviour {
    // Use this for initialization
    private void Start() {
    }

    // Update is called once per frame
    private void Update() {
    }
}
